import numpy as np

# Создаем матрицу A
A = np.array([[1, 2], [3, 4]])

# Создаем вектор b
b = np.array([5, 6])

# Решаем уравнение Ax = b
x = np.linalg.solve(A, b)

print("Решение уравнения Ax = b:")
print(x)
